namespace Staging.API.Infrastructure.BackgroundProcessing.Sync;

public sealed class SyncWorkerOptions : WorkerOptions
{
    // Intentionally empty for now
    // Add sync-specific options later if needed
}